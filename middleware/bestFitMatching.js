const asyncHandler = require('express-async-handler') ;

const bestFitAlgo = asyncHandler(async(skillNeedArray, skillServeArray)=>{
      const skill = [] ;
      // for(let i=0;i<skillNeedArray.length; ++i){
      //       for(let j = 0; j<skill)
      // }
}) ;